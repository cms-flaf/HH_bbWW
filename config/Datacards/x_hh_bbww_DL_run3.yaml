analysis: hh_bbww
type: resonant
model:
  parameters: [ MX ]
  param_dependent_bkg: true
  input_file_pattern: ${ERA}/DNN_m${MX}_vs_HME/DNN_m${MX}_vs_HME.root
signalFractionForRelevantBins: 0.2
# Every era listed here gets its own binning, its own datacards and its own limit.
#
# The four sub-eras are off for now. None of them builds standalone: on a single era's
# statistics a handful of DY shapes go negative beyond tolerance in the sparsest DNN
# slices, and resolveNegativeBins rejects them. Measured over 4 mass points, of ~500
# background shapes each -- 2022: 5 rejected, 2022EE: 5, 2023: 2, 2023BPix: 6, and
# Run3_Early: 0. It does not track luminosity; it tracks where DY's negative-weight
# events land. Turning them back on wants either min_slice_bkg_each_neff on in
# config/Datacards/binning_2d.yaml, so those slices are never cut, or a looser
# max_n_sigma_for_negative_bins for DY.
#
# They stay in era_groups below regardless: Run3_Early is the combination of all four
# datasets, which is a statement about the data, not about which limits we set.
eras:
  # - Run3_2022
  # - Run3_2022EE
  # - Run3_2023
  # - Run3_2023BPix
  - Run3_Early
era_groups:
  Run3_Early:
    - Run3_2022
    - Run3_2022EE
    - Run3_2023
    - Run3_2023BPix
channels:
  - muMu
  - eMu
  - eE
# The datacard bins, exactly as they exist in the input shapes. These are what
# StatInference/bin_opt_2d/rebin_2d.py wrote when it cut each base category into DNN
# slices -- the chain does no slicing of its own, it reads the directories it is given.
categories:
  - SR/res2b_dnn0
  - SR/res2b_dnn1
  - SR/res2b_dnn2
  - SR/res2b_dnn3
  - SR/boosted_dnn0
  - SR/boosted_dnn1
  - SR/boosted_dnn2
  - SR/boosted_dnn3
  - SR/recovery_dnn0
  - SR/recovery_dnn1
  - SR/recovery_dnn2
  - SR/recovery_dnn3

# How to take those names apart again, to group the slices of one base category for the
# per-category limits. Must match the category_pattern rebin_2d.py wrote them with.
category_pattern: "{base_category}_dnn{slice_idx}"

# Applied to the merged histograms before the datacards are built, by
# PreprocessShapesTask. The chain knows nothing about what this does -- it supplies
# --input, --output, --era and --config, and the script writes
# "<era>/<variable>/<variable>.root". Here it cuts the 2D DNN-vs-HME shapes into
# per-slice 1D ones; the knobs that steer that are in binning_2d.yaml, and the binning it
# derives is written beside the shapes it produced. Remove this block and the chain reads
# the merged histograms unchanged.
preprocess:
  script: StatInference/bin_opt_2d/rebin_2d.py
  args:
    - --binning-config
    - config/Datacards/binning_2d.yaml

processes:
  - process: data_obs
    is_data: true
    is_asimov_data: true

  - process: XtoHHto2B2W_2L_${MX} # GluGluToRadion2L_${MX}
    hist_name: XtoHHto2B2W_2L_${MX} # GluGluToRadion2L_${MX}
    param_values: [ 300, 400, 500, 550, 600, 650, 700, 800, 900, 1000 ]
    is_signal: true
    allow_zero_integral: true

  - process: XtoHHto2Tau2B_${MX}
    hist_name: XtoHHto2Tau2B_${MX}
    param_values: [ 300, 400, 500, 550, 600, 650, 700, 800, 900, 1000 ]
    is_signal: true
    allow_zero_integral: true
    allow_negative_bins_within_error: true
    max_n_sigma_for_negative_bins: 2

  - process: TT
    allow_negative_bins_within_error: true
    max_n_sigma_for_negative_bins: 2

  - process: DY
    channels:
      - eE
      - muMu
    allow_negative_bins_within_error: true
    max_n_sigma_for_negative_bins: 2

  - process: VV
    allow_negative_bins_within_error: true
    max_n_sigma_for_negative_bins: 2
    allow_zero_integral: true

  - process: ST
    allow_negative_bins_within_error: true
    max_n_sigma_for_negative_bins: 2

  # Single merged background template for the boosted slices, all three channels.
  #
  # Why: DY has no usable MC statistics in the b-tagged same-flavour categories.
  # Measured on Run3_Early (v2605a), summed over the four sub-eras, DY's effective
  # MC count (yield/error)^2 has a median of 5.8 across the 232 eE/muMu SR slices
  # and sits below the binning's own min_slice_bkg_neff of 4 in 42% of them, with several
  # slices going negative outright. TT, ST and VV are all fine on their own (median
  # N_eff 471 / 211 / 97, never below 4). So this is a DY problem, not a binning
  # granularity problem -- there is no 4-slice DNN binning of eE/muMu in which DY is
  # individually well measured, and merging DNN slices to chase it only buys a
  # positive number that is still statistically meaningless.
  #
  # Why all four rather than only the small ones: over the 64 boosted slices,
  # merging DY+ST+VV alone leaves 2 negative bins (eE/SR/boosted_dnn1 at m500);
  # including TT leaves none anywhere. Those last 2 bins are consistent with zero
  # and have 10x their size in positive donors, but the mass axis is binned by equal
  # signal quantiles, so every bin of an n<=5 bin slice holds >= 1/n >= 20% of the
  # signal and is therefore "relevant" under signalFractionForRelevantBins: 0.2 --
  # and resolveNegativeBins rejects a negative relevant bin unconditionally,
  # regardless of allow_negative_bins_within_error. Merging TT in as well avoids
  # relying on that interaction at all: no negative bins, nothing to tolerate.
  #
  # Cost, stated plainly: boosted has one background rate instead of four, so TT
  # cannot float independently there and the template's composition is frozen at the
  # MC prediction. Accepted because boosted holds 2-80 background events per slice
  # with no dedicated control region, and because autoMCStats (threshold 20, mode 1)
  # was already pooling these bins' MC-stat uncertainty across processes.
  #
  # `subprocesses` naming other datacard processes replaces them here, so TT, DY, ST
  # and VV keep their configuration above and simply do not enter the boosted bins.
  #
  # eMu is included as well. DY is restricted to eE/muMu in its own entry above and
  # was therefore missing from eMu's boosted background entirely -- not because it is
  # negligible there: over the 32 eMu boosted slices DY is 0.5% to 32% of the total
  # background (median 6.5%), with N_eff 1.2-22 (median 5.9, a third of slices below
  # 4). Merging is what lets it be counted, since the merged shape is assembled from
  # subprocess histograms by path and does not consult DY's own channel list. So eMu
  # boosted gains a real background it was previously missing, its DY never has to
  # stand on its own statistics, and all three channels now describe their boosted
  # background the same way. Folding DY in leaves zero negative bins in all 32 eMu
  # slices. DY remains absent from eMu res2b/recovery, which are unchanged.
  #
  # Caveat: QCD_scale_TT/DY/ST/VV each match this process through its subprocesses
  # (Uncertainty.appliesTo) and so apply to the whole merged yield rather than to
  # their own constituent's share. TT dominates boosted, so QCD_scale_TT is roughly
  # right; the other three over-cover. Net effect is a ~4.4% XS uncertainty where a
  # yield-weighted treatment would give ~3% -- conservative, and small next to the
  # MC-statistical noise this merge exists to remove.
  #
  # No negative-bin tolerance is set below on purpose: there are no negative bins to
  # tolerate, and this template builds under the unmodified resolveNegativeBins.
  - process: TotalBkg
    subprocesses:
      - TT
      - DY
      - ST
      - VV
    categories:
      - SR/boosted

  # - W
  # - Other

uncertainties:
  # Luminosity, as the Run 3 recommendation splits it: one component correlated across
  # all four eras but with a different size in each year group, and a second carried by
  # 2023 alone. Not a per-year uncorrelated pair -- that would treat the two year groups
  # as independent, which they are not.
  #
  # lumi_1 is one nuisance whose value depends on the era. Under a meta-era the datacard
  # maker cannot express that as a single lnN on the summed yield, so it builds the
  # variation sub-era by sub-era and writes it as a shape; each sub-era contributes its
  # own factor, and the ratio to nominal comes out as the yield-weighted blend.
  #
  # Reference: https://twiki.cern.ch/twiki/bin/viewauth/CMS/LumiRecommendationsRun3?extralog=-%20caching%20topic#Correlations_between_years_and_c
  - name: lumi_1_13p6TeV
    hasMultipleValues: True
    type: lnN
    value:
      - value: 0.0138
        type: lnN
        eras:
          - Run3_2022
          - Run3_2022EE
      - value: 0.0017
        type: lnN
        eras:
          - Run3_2023
          - Run3_2023BPix
  - name: lumi_2_13p6TeV
    type: lnN
    eras:
      - Run3_2023
      - Run3_2023BPix
    value: 0.0127

  # Muon ID
  - name: CMS_eff_m_id_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_eff_m_id_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_eff_m_id_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_eff_m_id_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # Muon Iso
  - name: CMS_eff_m_id_iso_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_eff_m_id_iso_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_eff_m_id_iso_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_eff_m_id_iso_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # Ele ID and Iso
  - name: EleID_TightIDIso_2022
    type: shape
    eras:
      - Run3_2022
  - name: EleID_TightIDIso_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: EleID_TightIDIso_2023
    type: shape
    eras:
      - Run3_2023
  - name: EleID_TightIDIso_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # Ele Trigger
  - name: SingleEleTrg_2022
    type: shape
    eras:
      - Run3_2022
  - name: SingleEleTrg_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: SingleEleTrg_2023
    type: shape
    eras:
      - Run3_2023
  - name: SingleEleTrg_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # Muon Trigger
  - name: SingleMuonTrg_2022
    type: shape
    eras:
      - Run3_2022
  - name: SingleMuonTrg_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: SingleMuonTrg_2023
    type: shape
    eras:
      - Run3_2023
  - name: SingleMuonTrg_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # Pileup
  - name: CMS_pileup_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_pileup_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_pileup_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_pileup_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # AK4 BTagging
  - name: CMS_btag_LF
    type: shape

  - name: CMS_btag_HF
    type: shape

  - name: CMS_btag_lfstats1_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_btag_lfstats1_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_btag_lfstats1_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_btag_lfstats1_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  - name: CMS_btag_lfstats2_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_btag_lfstats2_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_btag_lfstats2_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_btag_lfstats2_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  - name: CMS_btag_hfstats1_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_btag_hfstats1_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_btag_hfstats1_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_btag_hfstats1_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  - name: CMS_btag_hfstats2_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_btag_hfstats2_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_btag_hfstats2_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_btag_hfstats2_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  - name: CMS_btag_cferr1
    type: shape

  - name: CMS_btag_cferr2
    type: shape

  # AK8 BTagging
  - name: CMS_bbww_ak8_Hbb_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_bbww_ak8_Hbb_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_bbww_ak8_Hbb_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_bbww_ak8_Hbb_2023BPix
    type: shape
    eras:
      - Run3_2023BPix
  - name: CMS_bbww_ak8_Hcc_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_bbww_ak8_Hcc_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_bbww_ak8_Hcc_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_bbww_ak8_Hcc_2023BPix
    type: shape
    eras:
      - Run3_2023BPix
  - name: CMS_bbww_ak8_tau21_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_bbww_ak8_tau21_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_bbww_ak8_tau21_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_bbww_ak8_tau21_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  - name: CMS_scale_j_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_scale_j_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_scale_j_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_scale_j_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  - name: CMS_res_j_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_res_j_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_res_j_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_res_j_2023BPix
    type: shape
    eras:
      - Run3_2023BPix
  - name: CMS_scale_e_2022
    type: shape
    eras:
      - Run3_2022
  - name: CMS_scale_e_2022EE
    type: shape
    eras:
      - Run3_2022EE
  - name: CMS_scale_e_2023
    type: shape
    eras:
      - Run3_2023
  - name: CMS_scale_e_2023BPix
    type: shape
    eras:
      - Run3_2023BPix

  # Parton shower ISR/FSR. No `eras:` and no `processes:` -- one correlated nuisance
  # each, applied to every process including signal, because it is one Pythia shower
  # setting varied in every sample. Shape-only: the inclusive yield is normalised away
  # in the producer, so what reaches the card is acceptance alone.
  #
  # Expect ps_fsr to be resolved to lnN or dropped in some categories by autolnNThr /
  # ignorelnNThr. For a shape-only nuisance that is correct: a category whose acceptance
  # barely moves should drop out.
  - name: ps_isr
    type: shape
  - name: ps_fsr
    type: shape


  # For multiple values, Konstantin says do [ down, up ]
  - name: QCD_scale_TT
    type: lnN
    processes:
      - TT
    value: [ -0.036, 0.025 ] # TT XS uncertainty in percentage from xsec yaml
    # TT:
    # crossSec: '923.6'
    # reference: https://twiki.cern.ch/twiki/bin/view/LHCPhysics/TtbarNNLO#Updated_reference_cross_sections
    # unc: +22.6 -33.4 (scale) +/-  22.8 (PDF alphas) -24.6 +25.4 (mass) (to be multiplied by the BR)

  - name: QCD_scale_DY
    type: lnN
    processes:
      - DY
    value: [ -0.011, 0.007 ] # DY XS uncertainty in percentage from xsec yaml
    # DY_NNLO_QCD+NLO_EW:
    # crossSec: 2094.2 * 3
    # reference: https://twiki.cern.ch/twiki/bin/viewauth/CMS/MATRIXCrossSectionsat13p6TeV
    # unc: +0.7% - 1.1%, +- 1.0% PDF alphas

  - name: QCD_scale_ST
    type: lnN
    processes:
      - ST
    value: [ -0.022, +0.022 ]
    # Taken from ST to tW (largest uncertainty on XS) needs to be updated for each decay process later

  - name: QCD_scale_VV
    type: lnN
    processes:
      - VV
    # WARNING: this is not a QCD scale uncertainty. XSDB's `unc` field is an absolute
    # uncertainty in pb on its own Monte-Carlo integration, and 0.005 is just that
    # number divided by the cross section (WW 0.3733/80.23 = 0.47%, WZ 0.1318/29.1 =
    # 0.45%, ZZ 0.0649/12.75 = 0.51%; FLAF/config/crossSections13p6TeV.yaml:1051-1209).
    # The real NNLO diboson scale uncertainty is several percent. Needs replacing with a
    # number from the diboson theory reference; left as-is rather than guessed.
    value: 0.005

  - name: PDF_alphas
    hasMultipleValues: True
    type: lnN
    value:
      - value: 0.025
        type: lnN
        processes:
          - TT
      - value: 0.01
        type: lnN
        processes:
          - DY
      - value: 0.027
        type: lnN
        processes:
          - ST

  - name: top_mass
    hasMultipleValues: True
    type: lnN
    value:
      - value: [ -0.027, 0.028 ]
        type: lnN
        processes:
          - TT
      - value: [ -0.015, 0.015 ] # Taken from tW for now (largest unc percentage)
        type: lnN
        processes:
          - ST

  # Higgs branching ratios: theory, so correlated across every era -- no `eras:` field,
  # which is what makes them apply everywhere. Values from
  # https://gitlab.cern.ch/hh/naming-conventions#experimental-uncertainties
  #
  # XtoHHto2B2W_1L is the single-lepton signal and is not a process of this dilepton
  # card; it belongs in these lists when the SL card gains them. Single Higgs and VH
  # likewise, once they are added as processes.
  #
  # The process patterns are regexes (a `processes:` entry beginning with ^ is matched
  # with re.match, anything else compared literally). They have to be: by the time an
  # uncertainty is matched, the signal's ${MX} has been resolved to the mass being built,
  # so the process is XtoHHto2B2W_2L_700 and a literal "XtoHHto2B2W_2L_${MX}" matches
  # nothing at all -- silently, since an uncertainty that applies to no process is simply
  # never added.
  - name: BR_hbb
    type: lnN
    processes:
      - ^XtoHHto2B2W_2L_
      - ^XtoHHto2Tau2B_
    value: [ -0.0126, 0.0124 ]
  - name: BR_hww
    type: lnN
    processes:
      - ^XtoHHto2B2W_2L_
    value: [ -0.0152, 0.0153 ]
  - name: BR_htt
    type: lnN
    processes:
      - ^XtoHHto2Tau2B_
    value: [ -0.0163, 0.0165 ]

autolnNThr: 0.1
asymlnNThr: 0.001
ignorelnNThr: 0.001
autoMCStats:
  apply: true
  threshold: 20
  apply_to_signal: true
  mode: 1

campaigns:
  Run3_Early: run3_early
  Run3_2022: "2022preEE"
  Run3_2022EE: "2022postEE"
  Run3_2023: "2023preBPix"
  Run3_2023BPix: "2023postBPix"

limit_plots:
  - name: run3_vs_run2
    # Draw 1/2 sigma bands
    bands: true
    datacards:
      - { name: "Early Run 3", glob: "*.txt" }
    lumi_projections:
      - name: "Early Run 3 scaled to 138 fb^{-1}"
        lumi_target: 138
    external_limits:
      - config/Datacards/run2_spin0_DL_external_limits.json
    plot_params: &limit_plot_style
      xsec: pb
      y_log: true
      x_min: 200
      x_max: 1000
      y_min: 0.001
      y_max: 100

  - name: channels
    datacards:
      - { name: "Combined", glob: "*.txt" }
      - { name: "ee", glob: "${ERA}/eE/*.txt" }
      - { name: "e#mu", glob: "${ERA}/eMu/*.txt" }
      - { name: "#mu#mu", glob: "${ERA}/muMu/*.txt" }
    external_limits:
      - config/Datacards/run2_spin0_DL_external_limits.json
    plot_params: *limit_plot_style

  - name: categories
    datacards:
      - { name: "Combined", glob: "*.txt" }
      - { name: "res 2b", glob: "${ERA}/categories/SR_res2b/*.txt" }
      - { name: "recovery", glob: "${ERA}/categories/SR_recovery/*.txt" }
      - { name: "boosted", glob: "${ERA}/categories/SR_boosted/*.txt" }
    external_limits:
      - config/Datacards/run2_spin0_DL_external_limits.json
    plot_params: *limit_plot_style

# Nuisance pulls and impacts, drawn by PlotPullsAndImpactsTask into
# <version>/ImpactPlots/<name>/<era>/<mass>/.
#
# This entry has no `glob:`, so it uses the combined card ResonantLimitsTask writes --
# every era and every category in one fit. To rank one category instead, give an entry a
# glob addressing its card, e.g.
#
#   - name: res2b
#     glob: "${ERA}/categories/SR_res2b/*.txt"
#
# The glob and the mass must select exactly one card: PullsAndImpacts fits a single
# workspace.
impact_plots:
  - name: combined
    masses: [ 300, 600, 800 ]
    # The Asimov dataset is built at this signal strength. It has to be near the limit to
    # mean anything: dhi's default is r=1, which at 300 (limit ~6.8) is an almost
    # invisible signal so the ranking is really the background-only one, and at 800
    # (limit ~0.065) is ten times more signal than can be excluded, which pins r and
    # collapses every impact towards zero. "limit" takes the expected limit at each mass
    # from the limits already merged for these cards; a number sets it explicitly.
    #
    # It reaches combine as --expectSignal through PullsAndImpacts' custom_args. It cannot
    # go through dhi's `parameter_values`: for a resonant search hh_model is NO_STR, and
    # POITask then hard-codes the joined parameter values to '""' and the output postfix
    # to r=1.0, so the value would be dropped without a word.
    #
    # WARNING: the fit's output path does not depend on it -- custom_args is significant
    # for task identity but not for the store path -- so changing this value does not move
    # the outputs. Clear <version>/PullsAndImpacts, MergePullsAndImpacts and
    # PlotPullsAndImpacts under inference/data/store before re-running with a different
    # one, or law will report the old fit as complete.
    poi_value: limit
    # Migrad fails on a few of these fits from a bad seed gradient -- at 300 it was
    # CMS_eff_m_id_iso_2023 -- and dhi aborts the whole merge rather than the branch, so
    # 80 converged fits produce nothing. MINIMIZER_no_analytic, which dhi suggests, tends
    # to move the failure to another parameter; falling back to Simplex converges it.
    # Anything here reaches combine through PullsAndImpacts' custom_args.
    fit_args:
      - --cminFallbackAlgo Minuit2,Simplex,0:0.1
    plot_params:
      # One combine job per nuisance per direction. `robust` would instead take a single
      # fit and invert the Hessian, which is far cheaper but drops any parameter it cannot
      # invert -- it silently lost all four CMS_scale_j_* on these cards. Per-parameter
      # fits are what a ranking should be built from.
      method: default
      order_by_impact: true
      # mc_stats would add a few hundred autoMCStats bins. If you turn it on, set
      # parameters_per_page as well (25 works) or the plot is one unreadable page -- the
      # task refuses the combination rather than drawing it.
      mc_stats: false
